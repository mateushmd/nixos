{ modulesPath, ... }:
{
  /*
  imports = [
    "${modulesPath}/virtualisation/qemu-vm.nix"
  ];

  virtualisation = { 
    sharedDirectories = {
      sops-keys = {
        source = "/var/lib/sops";
        target = "/var/lib/sops";
      };
    };
    vmVariant.virtualisation = {
      memorySize = 4096;  
      cores = 4;
    };
  };
  */
}
